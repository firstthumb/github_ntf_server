type Query {
  repo(id: ID): Repo
  repositories(query: String): [Repo!]!
}

type Repo {
  repo_id: ID!
  name: String!
  owner: String!
  owner_id: Int!
  description: String!
  language: String!
  created_at: Date!
}